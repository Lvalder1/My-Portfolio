import React, { Component } from "react";
import styles from "./Projects.module.scss";

export default class Projects extends Component {
    render () {
        return (
            <div className={styles.projects} id="projects">Projects</div>
        );
    }
}